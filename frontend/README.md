# Frontend note

This directory is no longer a second UI implementation.

The runtime frontend source of truth is:

- `backend/templates/index.html`
- `backend/static/js/app.js`
- `backend/static/css/style.css`

The old duplicated standalone frontend files were removed to avoid drift.
