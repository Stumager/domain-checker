Flask==3.1.3
# Pinned explicitly: Flask only requires a floor, and an unpinned Werkzeug has
# broken Flask apps on major bumps before.
Werkzeug==3.1.8
Flask-Cors==6.0.5
requests==2.34.2
dnspython==2.8.0
python-dotenv==1.2.2
countryinfo==1.0.1
